diff -ruN /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/CMakeLists.txt /mnt/data/corum_alpha5_work/corum-ranked-mod/CMakeLists.txt
--- /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/CMakeLists.txt	2026-08-20 07:46:12.000000000 +0000
+++ /mnt/data/corum_alpha5_work/corum-ranked-mod/CMakeLists.txt	2026-08-20 15:55:27.097514113 +0000
@@ -21,6 +21,10 @@
 )
 
 file(GLOB_RECURSE SOURCES CONFIGURE_DEPENDS src/*.cpp)
+# alpha.5 canonical Debug Bot UI lives at src/DebugBotPopup.cpp. Some alpha.4
+# working trees may still contain the old src/debug/DebugBotPopup.cpp path;
+# ignore that stale duplicate so an overlay update cannot compile/link both copies.
+list(FILTER SOURCES EXCLUDE REGEX ".*/src/debug/DebugBotPopup\\.cpp$")
 add_library(${PROJECT_NAME} SHARED ${SOURCES})
 
 if (CORUM_RANKED_DEBUG_BOT_MATCH)
diff -ruN /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/about.md /mnt/data/corum_alpha5_work/corum-ranked-mod/about.md
--- /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/about.md	2026-08-20 07:50:00.000000000 +0000
+++ /mnt/data/corum_alpha5_work/corum-ranked-mod/about.md	2026-08-20 15:54:37.139606123 +0000
@@ -10,7 +10,7 @@
 
 Set **Ranked server URL** in this mod's settings. Production credentials and webhook secrets do not belong in the mod or repository.
 
-The `v0.4.0-alpha.4` development build can include **DEBUG BOT MATCH** behind
+The `v0.4.0-alpha.5` development build can include **DEBUG BOT MATCH** behind
 `CORUM_RANKED_DEBUG_BOT_MATCH`. Its results intentionally use the same live Ranked rating and
 statistics path during alpha testing. Build with that CMake option OFF and disable the matching
 server environment flag before a production release.
diff -ruN /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/changelog.md /mnt/data/corum_alpha5_work/corum-ranked-mod/changelog.md
--- /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/changelog.md	2026-08-20 07:46:14.000000000 +0000
+++ /mnt/data/corum_alpha5_work/corum-ranked-mod/changelog.md	2026-08-20 15:55:27.097699228 +0000
@@ -1,5 +1,13 @@
 # Changelog
 
+## v0.4.0-alpha.5
+
+- Fixed Debug Bot Match client/runtime API compatibility so both the current single-options call and the alpha.4 two-argument call compile.
+- Excluded the stale alpha.4 `src/debug/DebugBotPopup.cpp` overlay path from CMake to prevent duplicate/legacy Debug Bot compilation.
+- The compatibility fix is platform-independent and applies to Windows, iOS, Android32, and Android64 builds.
+- Kept the server-side debug password validation and did not bypass the debug gate to fix compilation.
+- Bumped the standalone Corum Ranked client/server workspace version to v0.4.0-alpha.5.
+
 ## v0.4.0-alpha.4
 
 - Added the compile-time-gated Debug Bot Match password/config UI and server-driven opponent simulator.
diff -ruN /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/mod.json /mnt/data/corum_alpha5_work/corum-ranked-mod/mod.json
--- /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/mod.json	2026-08-20 07:46:10.000000000 +0000
+++ /mnt/data/corum_alpha5_work/corum-ranked-mod/mod.json	2026-08-20 15:54:37.137952880 +0000
@@ -8,7 +8,7 @@
   },
   "id": "hwanhee1.corum_ranked",
   "name": "Corum Ranked",
-  "version": "v0.4.0-alpha.4",
+  "version": "v0.4.0-alpha.5",
   "developer": "hwanhee1",
   "description": "A separate server-authoritative Corum Ranked client. It does not share runtime state or UI with Corum Integration.",
   "dependencies": {
diff -ruN /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/src/RankedRuntime.cpp /mnt/data/corum_alpha5_work/corum-ranked-mod/src/RankedRuntime.cpp
--- /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/src/RankedRuntime.cpp	2026-08-20 08:18:28.000000000 +0000
+++ /mnt/data/corum_alpha5_work/corum-ranked-mod/src/RankedRuntime.cpp	2026-08-20 15:54:37.137769628 +0000
@@ -429,6 +429,11 @@
 }
 
 #if defined(CORUM_RANKED_DEBUG_BOT_MATCH)
+void RankedRuntime::startDebugBotMatch(std::string const& password, DebugBotMatchOptions options) {
+    options.password = password;
+    startDebugBotMatch(options);
+}
+
 void RankedRuntime::startDebugBotMatch(DebugBotMatchOptions options) {
     if (m_controlBusy || m_sessionToken.empty() || m_view.stage != RuntimeStage::Ready) return;
     m_installedMods = captureInstalledMods();
diff -ruN /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/src/RankedRuntime.hpp /mnt/data/corum_alpha5_work/corum-ranked-mod/src/RankedRuntime.hpp
--- /mnt/data/corum_alpha4_original_for_diff/corum-ranked-mod/src/RankedRuntime.hpp	2026-08-20 08:18:26.000000000 +0000
+++ /mnt/data/corum_alpha5_work/corum-ranked-mod/src/RankedRuntime.hpp	2026-08-20 15:54:37.137374141 +0000
@@ -98,7 +98,11 @@
     void submitReady();
     void submitBan(std::optional<std::string> canonicalLevelId);
 #if defined(CORUM_RANKED_DEBUG_BOT_MATCH)
+    // Primary API: password travels inside the options object.
     void startDebugBotMatch(DebugBotMatchOptions options);
+    // Compatibility overload for alpha.4 callers that still pass password separately.
+    // Keep this through alpha.5 so stale DebugBotPopup layouts compile on every target.
+    void startDebugBotMatch(std::string const& password, DebugBotMatchOptions options);
 #endif
 
     [[nodiscard]] RuntimeView const& view() const;
diff -ruN /mnt/data/corum_alpha4_original_for_diff/ranked/README.md /mnt/data/corum_alpha5_work/ranked/README.md
--- /mnt/data/corum_alpha4_original_for_diff/ranked/README.md	2026-08-20 08:20:24.000000000 +0000
+++ /mnt/data/corum_alpha5_work/ranked/README.md	2026-08-20 15:54:37.139867962 +0000
@@ -53,7 +53,7 @@
 운영 절차와 장애 시 동작은 [`docs/operations.md`](docs/operations.md), API 계약은 [`docs/api.md`](docs/api.md), 보안 경계는 [`docs/security.md`](docs/security.md)를 참고하세요.
 
 무료 alpha 호스팅 순서는 [`docs/free-hosting-koyeb-neon.md`](docs/free-hosting-koyeb-neon.md),
-이번 전체 소스에서 사용자가 실제로 해야 할 일은 [`docs/alpha-4-handoff.md`](docs/alpha-4-handoff.md),
+이번 전체 소스에서 사용자가 실제로 해야 할 일은 [`docs/alpha-5-handoff.md`](docs/alpha-5-handoff.md),
 Debug Bot Match 제거 방법은 [`docs/debug-bot-match.md`](docs/debug-bot-match.md)를 참고하세요.
 실제 변경 파일·구현 방식·테스트/빌드 결과는
-[`docs/v0.4.0-alpha.4-report.md`](docs/v0.4.0-alpha.4-report.md)에 정리되어 있습니다.
+[`docs/v0.4.0-alpha.5-report.md`](docs/v0.4.0-alpha.5-report.md)에 정리되어 있습니다.
diff -ruN /mnt/data/corum_alpha4_original_for_diff/ranked/apps/server/package.json /mnt/data/corum_alpha5_work/ranked/apps/server/package.json
--- /mnt/data/corum_alpha4_original_for_diff/ranked/apps/server/package.json	2026-08-20 07:45:50.000000000 +0000
+++ /mnt/data/corum_alpha5_work/ranked/apps/server/package.json	2026-08-20 15:54:37.138244172 +0000
@@ -1,6 +1,6 @@
 {
   "name": "@corum-ranked/server",
-  "version": "0.4.0-alpha.4",
+  "version": "0.4.0-alpha.5",
   "private": true,
   "type": "module",
   "scripts": {
@@ -11,7 +11,7 @@
     "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit"
   },
   "dependencies": {
-    "@corum-ranked/rules": "0.4.0-alpha.4",
+    "@corum-ranked/rules": "0.4.0-alpha.5",
     "@nestjs/common": "11.2.1",
     "@nestjs/core": "11.2.1",
     "@nestjs/platform-express": "11.2.1",
diff -ruN /mnt/data/corum_alpha4_original_for_diff/ranked/apps/server/test/debug-bot.integration.test.ts /mnt/data/corum_alpha5_work/ranked/apps/server/test/debug-bot.integration.test.ts
--- /mnt/data/corum_alpha4_original_for_diff/ranked/apps/server/test/debug-bot.integration.test.ts	2026-08-20 08:16:02.000000000 +0000
+++ /mnt/data/corum_alpha5_work/ranked/apps/server/test/debug-bot.integration.test.ts	2026-08-20 15:54:37.139267450 +0000
@@ -55,7 +55,7 @@
 const cleanMods: CreateSessionDto["installedMods"] = [
   {
     id: "hwanhee1.corum_ranked",
-    version: "v0.4.0-alpha.4",
+    version: "v0.4.0-alpha.5",
     enabled: true,
     loaded: true,
     internal: false,
@@ -134,7 +134,7 @@
     const created = await sessions.create({
       gdAccountId: accountId,
       gdUsername: `Debug${accountId}`,
-      clientVersion: "v0.4.0-alpha.4",
+      clientVersion: "v0.4.0-alpha.5",
       installedMods: structuredClone(cleanMods),
     });
     const result = await database.query<{
diff -ruN /mnt/data/corum_alpha4_original_for_diff/ranked/docs/alpha-5-handoff.md /mnt/data/corum_alpha5_work/ranked/docs/alpha-5-handoff.md
--- /mnt/data/corum_alpha4_original_for_diff/ranked/docs/alpha-5-handoff.md	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/corum_alpha5_work/ranked/docs/alpha-5-handoff.md	2026-08-20 15:54:37.139764107 +0000
@@ -0,0 +1,52 @@
+# v0.4.0-alpha.5 최초 배포 체크리스트
+
+현재 `Code.gs`와 `RankedConfig.gs`를 한 번도 반영하지 않은 최초 배포 기준이다.
+
+## 사용자가 해야 하는 일
+
+1. 전달받은 전체 소스 ZIP을 저장소 루트에 풀고 GitHub에 전체 업로드한다.
+2. 기존 Spreadsheet의 Apps Script 편집기에서 현재 `Code.gs`를
+   `apps-script/Code.gs` 전체 내용으로 교체한다.
+3. 같은 Apps Script 프로젝트에 `RankedConfig.gs` 파일을 새로 만들고
+   `apps-script/RankedConfig.gs` 전체 내용을 붙여 넣는다.
+4. `appsscript.json`을 표시해 저장소 파일과 맞춘다.
+5. `setupCorumIntegration()`을 한 번 실행한다.
+6. `setupCorumRankedConfig()`을 한 번 실행한다.
+7. 기존 맵 시트(`sheet1`) 오른쪽에 생긴 `Ranked Pool (1~6)`과 `Qualifying %`를
+   같은 맵 행에 입력한다. Pool이 빈 행은 Ranked에서 제외된다.
+8. 새로 생긴 `Ranked Tiers`, `Ranked CSMP Seed`, `Ranked Allowed Mods`,
+   `Ranked Config`의 미확정 운영값을 입력한다. 값을 임의로 정하지 않는다.
+9. `/exec?action=ranked_config` 응답의 `validation.valid=true`를 확인하고 마지막에만
+   `enabled=TRUE`로 바꾼 뒤 Apps Script를 새 버전으로 재배포한다.
+10. Neon Free DB를 만들고 `ranked/migrations/0001_initial_ranked.sql`을 한 번 실행한다.
+11. Koyeb Free Web Service를 GitHub 저장소에 연결하고
+    `ranked/docs/free-hosting-koyeb-neon.md`의 command/환경변수를 입력한다.
+12. Koyeb `/health`, `/ready`, `/api/ranked/config`를 확인한다.
+13. Geode alpha build를 설치하고 모드 설정의 서버 URL에 Koyeb HTTPS base URL을 넣는다.
+14. 반드시 테스트 계정으로 Debug Bot Match부터 확인한다. 이 버전의 Bot Match도
+    MMR/Score/Placement/W/L에 실제 반영된다.
+
+## source에서 직접 바꿔야 하는 값
+
+코드에 production secret을 넣을 필요는 없다. 배포 화면/Spreadsheet에서만 다음을 정한다.
+
+- Spreadsheet: Seed MMR, 티어 경계, K-factor, Placement 경기 수, timeout/실패 정책
+- Koyeb secret: Neon `DATABASE_URL`, `RANKED_SESSION_TOKEN_SECRET`
+- 선택: Discord webhook과 relay 설정
+- alpha Bot: `ENABLE_DEBUG_BOT_MATCH=true`, `DEBUG_BOT_PASSWORD=2008`
+
+`2008`은 개발 gate일 뿐 계정 인증 수단이 아니다. 정식 출시 전에 Bot 기능 전체를 끈다.
+
+## 이후 파일 변경 시 적용 위치
+
+| 저장소 경로 | 사용자가 실제로 반영할 곳 |
+| --- | --- |
+| `apps-script/Code.gs` | 기존 Apps Script의 `Code.gs` 전체 교체 후 새 배포 |
+| `apps-script/RankedConfig.gs` | 같은 Apps Script의 동명 파일 전체 교체/생성 후 새 배포 |
+| `ranked/apps/server/**`, `ranked/packages/**`, `ranked/migrations/**` | GitHub push → Koyeb 재배포; 새 migration이 생긴 버전만 DB에 순서대로 실행 |
+| `corum-ranked-mod/**` | GitHub Actions/로컬에서 새 `.geode` 빌드 후 설치·배포 |
+| `.github/workflows/**` | GitHub push만 하면 적용 |
+| `ranked/docs/**` | 배포 명령/운영 절차 확인용 |
+
+앞으로 전달본은 일부 overlay가 아니라 매 버전 전체 소스 ZIP이며, 이 ZIP 역시 저장소
+루트에 바로 덮어쓸 수 있는 구조로 만든다.
diff -ruN /mnt/data/corum_alpha4_original_for_diff/ranked/docs/api.md /mnt/data/corum_alpha5_work/ranked/docs/api.md
--- /mnt/data/corum_alpha4_original_for_diff/ranked/docs/api.md	2026-08-20 07:49:54.000000000 +0000
+++ /mnt/data/corum_alpha5_work/ranked/docs/api.md	2026-08-20 15:54:37.139379727 +0000
@@ -18,11 +18,11 @@
 {
   "gdAccountId": "12345",
   "gdUsername": "Player",
-  "clientVersion": "v0.4.0-alpha.4",
+  "clientVersion": "v0.4.0-alpha.5",
   "installedMods": [
     {
       "id": "hwanhee1.corum_ranked",
-      "version": "v0.4.0-alpha.4",
+      "version": "v0.4.0-alpha.5",
       "enabled": true,
       "loaded": true,
       "internal": false,
diff -ruN /mnt/data/corum_alpha4_original_for_diff/ranked/docs/debug-bot-match.md /mnt/data/corum_alpha5_work/ranked/docs/debug-bot-match.md
--- /mnt/data/corum_alpha4_original_for_diff/ranked/docs/debug-bot-match.md	2026-08-20 07:49:10.000000000 +0000
+++ /mnt/data/corum_alpha5_work/ranked/docs/debug-bot-match.md	2026-08-20 15:54:37.139475539 +0000
@@ -1,6 +1,6 @@
 # Debug Bot Match 개발 문서
 
-`v0.4.0-alpha.4`의 Bot Match는 실제 Ranked 엔진을 시험하는 임시 개발 harness다.
+`v0.4.0-alpha.5`의 Bot Match는 실제 Ranked 엔진을 시험하는 임시 개발 harness다.
 비밀번호 입력값은 URL, 서버 로그, 일반 로그에 기록하지 않으며 HTTPS JSON body로만 보낸다.
 
 ## 활성화
diff -ruN /mnt/data/corum_alpha4_original_for_diff/ranked/docs/v0.4.0-alpha.5-report.md /mnt/data/corum_alpha5_work/ranked/docs/v0.4.0-alpha.5-report.md
--- /mnt/data/corum_alpha4_original_for_diff/ranked/docs/v0.4.0-alpha.5-report.md	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/corum_alpha5_work/ranked/docs/v0.4.0-alpha.5-report.md	2026-08-20 15:55:27.097651807 +0000
@@ -0,0 +1,24 @@
+# v0.4.0-alpha.5 빌드 수정 보고서
+
+## 원인
+
+alpha.4 계열 Debug Bot UI 중 일부 호출부는 `startDebugBotMatch(password, options)`를 호출하지만,
+`RankedRuntime`의 최신 선언/정의는 `startDebugBotMatch(options)` 하나만 제공해 모든 Geode 타깃에서
+`too many arguments to function call` 컴파일 오류가 발생할 수 있었다. 이 오류는 Android64 전용이 아니라
+공통 C++ 소스의 API 불일치이므로 Windows/iOS/Android32/Android64 모두에 영향을 준다.
+
+## alpha.5 수정
+
+- 기본 API `startDebugBotMatch(DebugBotMatchOptions options)`는 그대로 유지한다.
+- CMake에서 alpha.4의 stale `src/debug/DebugBotPopup.cpp` 경로를 빌드 대상에서 제외해, 덮어쓰기 업데이트 후 중복 DebugBotPopup이 남아 있어도 공통 빌드가 깨지지 않게 했다.
+- 호환 overload `startDebugBotMatch(std::string const& password, DebugBotMatchOptions options)`를 추가한다.
+- 호환 overload는 전달받은 password를 `options.password`에 넣은 뒤 기본 API로 위임한다.
+- 따라서 새 UI의 단일-options 호출과 이전 alpha.4 UI의 2-argument 호출이 모두 같은 서버 요청 경로를 사용한다.
+- password 검증을 제거하거나 우회하지 않는다.
+- manifest/server/rules workspace 버전을 `v0.4.0-alpha.5` / `0.4.0-alpha.5`로 맞췄다.
+
+## 검증 범위
+
+이 전달본에서는 Geode SDK가 로컬 환경에 없어 플랫폼별 `.geode` 바이너리를 직접 생성하지 않았다.
+대신 저장소에 포함된 C++23 domain test, Node rules/server tests, typecheck/build, Apps Script 검증을 실행한다.
+최종 Windows/iOS/Android32/Android64 바이너리는 기존 `build-ranked-mod.yml` matrix에서 검증해야 한다.
diff -ruN /mnt/data/corum_alpha4_original_for_diff/ranked/package-lock.json /mnt/data/corum_alpha5_work/ranked/package-lock.json
--- /mnt/data/corum_alpha4_original_for_diff/ranked/package-lock.json	2026-08-20 07:46:06.000000000 +0000
+++ /mnt/data/corum_alpha5_work/ranked/package-lock.json	2026-08-20 15:54:37.139067864 +0000
@@ -1,12 +1,12 @@
 {
   "name": "corum-ranked",
-  "version": "0.4.0-alpha.4",
+  "version": "0.4.0-alpha.5",
   "lockfileVersion": 3,
   "requires": true,
   "packages": {
     "": {
       "name": "corum-ranked",
-      "version": "0.4.0-alpha.4",
+      "version": "0.4.0-alpha.5",
       "workspaces": [
         "packages/*",
         "apps/*",
@@ -22,9 +22,9 @@
     },
     "apps/server": {
       "name": "@corum-ranked/server",
-      "version": "0.4.0-alpha.4",
+      "version": "0.4.0-alpha.5",
       "dependencies": {
-        "@corum-ranked/rules": "0.4.0-alpha.4",
+        "@corum-ranked/rules": "0.4.0-alpha.5",
         "@nestjs/common": "11.2.1",
         "@nestjs/core": "11.2.1",
         "@nestjs/platform-express": "11.2.1",
@@ -3194,7 +3194,7 @@
     },
     "packages/rules": {
       "name": "@corum-ranked/rules",
-      "version": "0.4.0-alpha.4"
+      "version": "0.4.0-alpha.5"
     }
   }
 }
diff -ruN /mnt/data/corum_alpha4_original_for_diff/ranked/package.json /mnt/data/corum_alpha5_work/ranked/package.json
--- /mnt/data/corum_alpha4_original_for_diff/ranked/package.json	2026-08-20 07:45:46.000000000 +0000
+++ /mnt/data/corum_alpha5_work/ranked/package.json	2026-08-20 15:54:37.138080058 +0000
@@ -1,7 +1,7 @@
 {
   "name": "corum-ranked",
   "private": true,
-  "version": "0.4.0-alpha.4",
+  "version": "0.4.0-alpha.5",
   "workspaces": [
     "packages/*",
     "apps/*",
diff -ruN /mnt/data/corum_alpha4_original_for_diff/ranked/packages/rules/package.json /mnt/data/corum_alpha5_work/ranked/packages/rules/package.json
--- /mnt/data/corum_alpha4_original_for_diff/ranked/packages/rules/package.json	2026-08-20 07:45:48.000000000 +0000
+++ /mnt/data/corum_alpha5_work/ranked/packages/rules/package.json	2026-08-20 15:54:37.138367936 +0000
@@ -1,6 +1,6 @@
 {
   "name": "@corum-ranked/rules",
-  "version": "0.4.0-alpha.4",
+  "version": "0.4.0-alpha.5",
   "private": true,
   "type": "module",
   "main": "./dist/index.js",
